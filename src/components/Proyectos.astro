---
const PROJECTS = [
  {
    title: "Proyecto 1",
    description:
      "Lorem ipsum dolor sit amet, consectetur adipisicing elit. Voluptate voluptatibus quidem quibusdam assumenda debitis labore sed repudiandae nobis ut a accusantium pariatur magni ea voluptatem ex eum, earum magnam vel?",
    link: "#",
    github: "#",
    image: "https://via.placeholder.com/300x200",
    tags: ["HTML", "CSS", "JavaScript"],
  },
];
---

{
  PROJECTS.map(({ title, description, tags, image }) => (
    <article>
      <h3>{title}</h3>
      <p>{description}</p>
      <ul>
        {tags.map((tag) => (
          <li>{tag}</li>
        ))}
      </ul>
      <img src={image} alt={title} />
    </article>
  ))
}
